#!/bin/bash

cd ~
if [ "$1" = "-p" ]; then
  srm -r go
  srm -r websites
  rm -rf .Private
  srm -r .ssh
  rm -rf .rbenv
  rm -rf .pyenv
  rm -rf .nodebrew
  srm .bash_history
  cd ~/setup-ubuntu
  ./apt-remove.sh
  cd ~
  srm -r setup-ubuntu
  history -c
  exit
fi


if [ "$1" = "-n" ]; then
  rm -rf go
  rm -rf websites
  rm -rf .Private
  rm -rf .ssh
  rm -rf .rbenv
  rm -rf .pyenv
  rm -rf .nodebrew
  rm .bash_history
  cd ~/setup-ubuntu
  ./apt-remove.sh
  cd ~
  rm -rf setup-ubuntu
  history -c
  exit
fi

